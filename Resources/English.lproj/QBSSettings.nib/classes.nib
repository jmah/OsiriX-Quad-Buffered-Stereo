{
    IBClasses = (
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {beginQuadBufferedAlertSheet = id; showSettingsPanel = id; }; 
            CLASS = QBSController; 
            LANGUAGE = ObjC; 
            OUTLETS = {creditsTextView = NSTextView; settingsPanel = NSPanel; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}